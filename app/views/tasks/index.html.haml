= render 'shared/projectbar'
.row
  .col-md-3
    = render partial: 'shared/projectstatuspanel', locals: { project: @project }
  .col-md-9
    .panel.panel-green.panel-default
      .panel-body
        = form_for_filterrific @filterrific, :html => { :class => 'form-horizontal' } do |f|
          .form-group
            .col-xs-12
              .input-group
                .input-group-btn
                  = link_to_button(reset_filterrific_url, "reset_filters", "remove")
                = f.text_field :search_title, :class => 'form-control filterrific-periodically-observed'
                - if thinker_signed_in?
                  .input-group-btn
                    = link_to_button(new_project_task_path, "new", "plus")
          = render partial: 'shared/filters', :locals => { :f => f }
          = render_filterrific_spinner
          .form-group
            .col-xs-12
              %table.table.table-responsive.table-hover#filterrific_results
                = render partial: 'list', locals: { tasks: @tasks }
        = paginate @tasks
