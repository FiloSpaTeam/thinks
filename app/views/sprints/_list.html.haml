%thead
  %tr
    %td.col-lg-10.col-md-10.col-sm-9.col-xs-9
      .col-xs-3
        %h5
          %strong
            = t "sprint"
%tbody
  - unless (@sprints.empty?)
    - @sprints.each do |sprint|
      Ciao
  - else
    %td.text-center
      = t('no_record')
