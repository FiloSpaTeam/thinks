%li.nav-item{ role: 'presentation' }
  = label_tag :closed, :class => "control-label nav-link" do
    = check_box_tag('filters[with_deleted_at]', true, filter_value(:with_deleted_at))
    = t "filters.closed"
