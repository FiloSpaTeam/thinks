= content_for :body do
  .site-wrapper
    .site-wrapper-inner
      .cover-container
        .masthead.clearfix
          .inner
            %h3.masthead-brand
              Thinks
            %nav
              %ul.nav.masthead-nav
                %li
                  = link_to("Home", root_path)
                %li
                  = link_to("About", how_does_it_works_url)

        .inner.cover
          = yield
